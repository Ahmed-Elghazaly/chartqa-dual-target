# Exact versions, so that "it worked yesterday" has an answer.
#
# HOW THIS FILE IS MAINTAINED
# ---------------------------
# The CPU block below is generated from the development virtualenv
# (Python 3.11) with `pip freeze` and is what CI installs.
#
# The GPU block is DELIBERATELY EMPTY until Phase 2. The training stack must be
# pinned to versions that actually work on the Kaggle T4 image, and that can
# only be measured there — pinning them from a laptop with no CUDA would be a
# guess written down as if it were a fact. Phase 2's smoke test emits the exact
# frozen list and it gets pasted in below.
#
# Known hard floor (verified in Phase 0): transformers >= 4.57.0. Qwen3-VL's
# config declares transformers_version 4.57.0.dev0 and earlier releases cannot
# load model_type "qwen3_vl" at all.
#
# ---------------------------------------------------------------- CPU / CI ---
PyYAML==6.0.3
Pygments==2.21.0
anyio==4.14.2
attrs==26.1.0
certifi==2026.7.22
charset-normalizer==3.5.1
click==8.4.2
contourpy==1.3.3
coverage==7.15.4
cycler==0.12.1
filelock==3.32.4
fonttools==4.63.0
fsspec==2026.7.0
h11==0.16.0
hf-xet==1.6.0
httpcore==1.0.9
httpx==0.28.1
huggingface_hub==1.28.0
idna==3.19
iniconfig==2.3.0
jsonschema-specifications==2025.9.1
jsonschema==4.26.0
kiwisolver==1.5.0
matplotlib==3.11.1
numpy==2.4.6
packaging==26.3
pandas==3.0.5
pillow==12.3.0
pluggy==1.6.0
pyparsing==3.3.2
pytest-cov==7.1.0
pytest==9.1.1
python-dateutil==2.9.0.post0
referencing==0.37.0
requests==2.34.2
rpds-py==2026.6.3
ruff==0.16.4
six==1.17.0
tqdm==4.70.0
typing_extensions==4.16.0
urllib3==2.7.0

# ------------------------------------------------------- GPU (Phase 2) ------
# transformers==     # >= 4.57.0 required for model_type "qwen3_vl"
# peft==
# trl==
# accelerate==
# bitsandbytes==
# torch==            # comes from the Kaggle/Colab base image; record, do not install
# unsloth==          # only if the unsloth backend is selected in Phase 2
