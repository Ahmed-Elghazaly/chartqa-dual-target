% tab_structured_cost — generated by: cdt-report --what structured_cost
% Do not edit by hand; edits are overwritten on the next build.
\begin{table}[t]
\centering
\caption{The cost of asking for a structured record instead of a bare answer, measured zero-shot.}
\label{tab:structured_cost}
\begin{tabular}{lcccc}
\toprule
Prompt & Relaxed accuracy & Median tokens & Median latency & Valid output \\
\midrule
Plain prompt (published) & \TODO{} & \TODO{} & \TODO{}\,s & \TODO{} \\
Structured record & \TODO{} & \TODO{} & \TODO{}\,s & \TODO{} \\
\bottomrule
\end{tabular}

\vspace{2pt}
\footnotesize{Both arms are the same checkpoint over the same items with the same greedy decoding; only the requested output format differs. Published measurements on comparable models report 4.0 to 8.9 points for asking for JSON at all, and this asks for considerably more than JSON. A large cost is a result, not a failure \textemdash{} it is the price the fine-tune has to earn back.}
\end{table}
