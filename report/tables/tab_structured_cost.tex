% tab_structured_cost — generated by: cdt-report --what structured_cost
% Do not edit by hand; edits are overwritten on the next build.
\begin{table}[t]
\centering
\caption{The cost of asking for a structured record instead of a bare answer, measured zero-shot on 1,920 ChartQA validation questions.}
\label{tab:structured_cost}
\begin{tabular}{lcccc}
\toprule
Prompt & Relaxed accuracy & Median tokens & Median latency & Valid output \\
\midrule
Plain prompt (published) & 72.55\,\tiny{[70.52, 74.53]} & 4 & 0.33\,s & \TODO{} \\
Structured record & 48.75\,\tiny{[46.56, 50.99]} & 178 & 10.49\,s & 67.5\% \\
\bottomrule
\end{tabular}

\vspace{2pt}
\footnotesize{Both arms are the same checkpoint over the same items with the same greedy decoding; only the requested output format differs. The structured arm costs \textbf{-23.80 points}. Published measurements on comparable models report 4.0 to 8.9 points for asking for JSON at all, and this asks for considerably more than JSON. A large cost is a result, not a failure \textemdash{} it is the price the fine-tune has to earn back.}
\end{table}
