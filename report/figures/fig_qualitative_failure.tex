% fig_qualitative_failure
\begin{figure}[t]
\centering
% \includegraphics[width=\linewidth]{figures/fig_qualitative_failure.pdf}
\caption{Failures, grouped by mode: evidence pointing at the wrong element, an element read correctly but the wrong operation applied, a tree that executes to a different number than the model stated, and output that is not a valid record at all. The last is counted as a failure, not discarded.}
\label{fig:qualitative_failure}
\end{figure}
