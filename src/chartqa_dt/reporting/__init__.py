"""Generated report artefacts — `PLAN.md` 10.1.

Every number the report prints is produced here from a recorded JSON file. Nothing measured
is typed into the LaTeX by hand, so a figure in the PDF cannot drift from
`verification/measured_facts.json`; and nothing unmeasured is rendered as a number, so a gap
shows up as red `\\TODO` on the page rather than as a plausible zero.
"""
